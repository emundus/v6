name: Border radius (button, module, ...)
description: Border radius
type: core

form:
  fields:

    default:
      type: input.text
      label: Border radius by default
      default: '8px'
    applicant:
      type: input.text
      label: Border radius for Applicant
      default: '8px'
    coordinator:
      type: input.text
      label: Border radius for Coordinator
      default: '8px'
    cards:
      type: input.text
      label: Border radius for Applicant cards
      default: '16px'
    coordinator-cards:
      type: input.text
      label: Border radius for Coordinator cards
      default: '16px'
